
engine = create_engine("postgres://postgres:postgres@localhost:5432/sql_hw")

cross join
same_sporder = session.query(EA, NA).filter(EA.sporder == NA.sporder).limit(10).all()

sql alchemy know dates, not sqllite

for dates
import datetime as dt

